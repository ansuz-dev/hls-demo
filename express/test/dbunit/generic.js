import db from "../../models/index.js";

const {Sequelize} = db;

const Op = Sequelize.Op;

const init = async () => {
};

const clean = async () => {
};

const dbunit = {
  init,
  clean,
};

export default dbunit;
